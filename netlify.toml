[build]
  command = "pnpm install && node ./build/generate-svg.js && node ./build/generate-astro.js && pnpm --filter @meteor-icons/docs build"
  publish = "docs/dist"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "10.33.0"
